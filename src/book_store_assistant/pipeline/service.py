from pathlib import Path

from book_store_assistant.models import BookRecord


def process_isbn_file(input_path: Path) -> list[BookRecord]:
    """Process a CSV file of ISBN values into structured book records."""
    raise NotImplementedError("Pipeline service is not implemented yet.")
